As the simpliywrting tailwind will fail in production enviroment
so we need to install all below dependencies
1.npm init -y
2.npm install -D tailwindcss postcss autoprefixer
3.npm i vite
4. then after that write "scripts":{
    "start":"vite"
  },
  in package.json file after that folow:>
5. npx tailwindcss init